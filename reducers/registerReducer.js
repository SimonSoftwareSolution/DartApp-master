
const initialRegisterState = {

}

export default (state = initialRegisterState, action) => {
    switch(action.type) 
    {
        case 'REGISTER_USER' :
         return state;
         default :
         return state;
    }
}
